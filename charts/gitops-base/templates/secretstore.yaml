apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: demo-store
  namespace: demo
spec:
  provider:
    kubernetes:
      remoteNamespace: demo
      auth:
        serviceAccount:
          name: secretstore-sa